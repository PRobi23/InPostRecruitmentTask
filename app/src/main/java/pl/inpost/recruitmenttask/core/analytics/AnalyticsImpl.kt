package pl.inpost.recruitmenttask.core.analytics

/**
 * This class would be responsible for analytics. Since this is just a test project
 * the implementation is not done. It needs to be implemented in the future.
 */
class AnalyticsImpl : Analytics {
    override fun logError() = Unit
    override fun logUserEvent() = Unit
}